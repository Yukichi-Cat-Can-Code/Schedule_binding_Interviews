from django.contrib import admin

# MongoDB data is managed through the REST API
# Django admin is used for user management only
# All interview scheduling data is in MongoDB and accessed via mongo_models.py
